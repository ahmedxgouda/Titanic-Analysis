[project]
name = "ml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "torch",
    "ipykernel",
    "jupyter",
    "scikit-learn",
    "torchvision",
]
